# Importable package: enable BLE on the ESP32 + instantiate the ble_keyboard.
#
# Add your `esp32: board:` and an `external_components:` (where you pin the
# release); this package supplies the ESP-IDF Bluetooth sdkconfig (the part that's
# easy to get wrong) and the keyboard component. Pair it with canonical.yaml:
#
#   substitutions:
#     ble_name: Living Room Remote     # the name the TV sees when pairing
#     ble_keyboard_id: kb
#   packages:
#     ble: github://HomeOps/esphome-blekeyboard/packages/ble.yaml@<ref>

esp32:
  framework:
    type: esp-idf
    sdkconfig_options:
      CONFIG_BT_ENABLED: y
      CONFIG_BT_CONTROLLER_ENABLED: y
      CONFIG_BT_BLUEDROID_ENABLED: y
      CONFIG_BT_NIMBLE_ENABLED: n
      CONFIG_BT_BLE_ENABLED: y
      CONFIG_BT_GATTS_ENABLE: y
      CONFIG_BT_BLE_42_FEATURES_SUPPORTED: y
      CONFIG_BT_BLE_50_FEATURES_SUPPORTED: n
      CONFIG_BT_BLE_42_ADV_EN: y
      CONFIG_BT_BLE_42_SCAN_EN: y
      CONFIG_BT_BLE_SMP_ENABLE: y
      CONFIG_BT_ACL_CONNECTIONS: "4"

ble_keyboard:
  id: ${ble_keyboard_id}
  device_name: "${ble_name}"
  host_slots: 4
